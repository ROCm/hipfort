resources:
  repositories:
  - repository: pipelines_repo
    type: github
    endpoint: ROCm
    name: ROCm/ROCm

variables:
- group: common
- template: /.azuredevops/variables-global.yml@pipelines_repo

# Pipeline disabled: hipfort depends on hipSPARSE and rocSPARSE (disabled)
trigger: none
pr: none

jobs:
  - template: ${{ variables.CI_COMPONENT_PATH }}/hipfort.yml@pipelines_repo

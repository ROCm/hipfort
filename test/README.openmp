The openmp directory contains tests that combine hipfort with OpenMP offload.
These are intended to be used with amdflang or any other compiler
using OpenMP offload. Note that they contain a different build mechanism than
the other testers as one needs to specify the architecture.
